# Render Blueprint - Deploy Django Backend
# KHÔNG dùng file này. Đọc hướng dẫn bên dưới.
# 
# ⚠️ Cách deploy đúng (không dùng render.yaml):
# 1. Vào Render Dashboard → + New → Web Service
# 2. Chọn repo: ln-khanhduy/elearning
# 3. Cấu hình:
#    - Name: elearning-backend
#    - Root Directory: backend    ← QUAN TRỌNG
#    - Runtime: Docker
#    - Region: Singapore
#    - Plan: Free
# 4. Add Environment Variables (thủ công)
# 5. Deploy